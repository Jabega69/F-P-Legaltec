RewriteEngine On

# Canonical domain: https://pflegaltec.com
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} ^www\.pflegaltec\.com$ [NC]
RewriteRule ^ https://pflegaltec.com%{REQUEST_URI} [R=301,L]

DirectoryIndex index.html
Options -Indexes
